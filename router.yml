headers:
  all:
    request:
      - propagate:
          matching: "^*"

include_subgraph_errors:
  all: true

telemetry:
  instrumentation:
    spans:
      mode: spec_compliant

#rhai:
#  main: main.rhai
#  scripts: rhai/

#apq:
#  router:
#    cache:
#      in_memory:
#          limit: 1
#      redis:
#        urls: [ "redis://localhost:6379" ]

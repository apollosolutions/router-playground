headers:
  all:
    request:
      - propagate:
          matching: "^*"

apq:
  router:
    cache:
      in_memory:
          limit: 1
      redis:
        urls:
          [
            "redis://localhost:6380",
          ]
